조건문 : C와 동일

반복문 : 
for i in 0...100 : 0~100까지 반복
for i in 0..<100 : 0~99까지 반복


list 개수 세는법 : list이름.count 


버튼 : 켜져있는 변수 하나 만들고 bool 값으로 제어
스위치 : outlet으로 선언해준 다음 isOn / isOff 로 제어


2.6 : 옵셔널, 클래스, 구조체
-옵셔널 : 널인지 값이 있는지 뭐가 있는지 모르겠다는 뜻. "없으면 어떡하지..?" 정도의 의문이 들 때 사용. 만약 null이 아니라는 확신이 있으면 ? 를 ! 로 바꾸면 됨

